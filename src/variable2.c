#include <stdio.h>
int main(void)
{
    int a,b,sum;
    
    printf("enter the 2 num: ");
    scanf("%d%d",&a,&b);
    printf("the number u entered are %d and %d\n",a,b);
    sum=a+b;
    printf("adding.........\n");
    printf("the sum is:%d",sum);

}